# SMS Service (Layer 2)

The SMS Service is responsible for handling the transmission of SMS messages between UEs. It operates as an application layer service on top of the core network, and provides a simple interface for sending and receiving SMS messages. The SMS Service will be implemented as a standalone server application that the CNG can route messages to and from.